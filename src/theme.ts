import { createTheme } from '@mui/material/styles'

// Material-UI theme configuration for the application
// Defines the visual design system including colors, typography, and component styling
export const theme = createTheme({
    palette: {
        mode: 'light', // Light theme mode
        primary: { main: '#1976d2' } // Primary brand color (blue)
    }
})